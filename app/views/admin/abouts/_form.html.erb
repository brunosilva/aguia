<%= form_with(model: [:admin, about]) do |form| %>
  <% if about.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(about.errors.count, "error") %> prohibited this about from being saved:</h2>

      <ul>
        <% about.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :aboutus, style: "display: block" %>
    <%= form.rich_text_area :aboutus, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :mission, style: "display: block" %>
    <%= form.rich_text_area :mission, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :vision, style: "display: block" %>
    <%= form.rich_text_area :vision, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :values, style: "display: block" %>
    <%= form.rich_text_area :values, class: 'form-control' %>
  </div>

  <div>
    <%= form.submit 'Salvar', class: 'btn btn-success' %>
  </div>
<% end %>
